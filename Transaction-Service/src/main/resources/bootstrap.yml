spring:
  application:
    name: transaction
  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        healthCheckPath: /health-check
        healthCheckInterval: 20s
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: configuration-server